repos:
  - repo: local
    hooks:
      - id: protect-workflows-do-not-change
        name: Protect workflows DoNotChange files
        entry: uv run --project workflows --no-sync python -m pipelines.shared.repo_protection
        language: system
        pass_filenames: false
        always_run: true
