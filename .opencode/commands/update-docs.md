---
description: update the documentation triad and indexes for a significant change
agent: docs-writer
---

Use the `docs-update-guard` skill and update documentation for: $ARGUMENTS

Required actions:

1. Determine whether the change is significant.
2. Update the impacted docs under `docs/planning/`, `docs/business/`, and `docs/developer/`.
3. Update all impacted `index.md` files.
4. Create or update an ADR under `docs/planning/adr/` if architecture or long-lived tradeoffs changed.
5. Summarize what changed and what still needs human review.
